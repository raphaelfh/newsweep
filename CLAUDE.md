# CLAUDE.md

Local next-edit autocomplete server for JetBrains IDEs. Serves sweep-next-edit-v2-7B (MLX 4-bit) via a Tabby-compatible API on port 8741. macOS Apple Silicon only.


Always update the documentation to be aligned with the project's code. Use the /doc folder. 

Always write everything in english.

